using System.Collections.Generic;
using UCMS.Model;

namespace UCMS.Data
{
    public interface IDataListDataAdapter
    {
        DataList Insert(DataList list);
        void Update(DataList list);
        void Delete(string id);
        DataList GetById(string id);
        List<DataList> Select(string typeId);
    }
}
