angular.module('uccm.app.cform')
    .controller('customer1Ctrl', function($scope, $http) {

        $scope.FormData = {};

        /* submit action - called to submit form */
        $scope.submit = function() {
            alert(JSON.stringify($scope.FormData));
        }
        
        
        $scope.save = function(){
           alert("hello world");
        }
         
        $scope.cancel = function () {

        }

        /* initialize method - called before the form loaded */
        $scope.init = function() {
            
        }
    });